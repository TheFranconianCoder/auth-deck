# Binaries and build output
/auth-deck
/dist/
*.exe
*.test
*.out

# Test and coverage artifacts
coverage.out
*.coverprofile

# AuthDeck token stores (may contain access/refresh tokens)
tokens.json
auth-deck-tokens.json
.auth-deck-tokens.json

# Local configuration and secrets
.env
.env.*
config.yaml
config.local.yaml
*.local.yaml

# OS and editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
